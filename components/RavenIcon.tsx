export default function RavenIcon({ size = 100 }: { size?: number }) {
  return (
    <span style={{ fontSize: size * 0.8, lineHeight: 1, display: 'inline-block', filter: 'drop-shadow(0 0 10px rgba(201,168,76,0.3))' }}>
      🐦‍⬛
    </span>
  )
}
